server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    # SPA fallback: a hard refresh on /invoices/42 must serve index.html, not 404.
    try_files $uri $uri/ /index.html;
  }

  # NOTE: deliberately no /api proxy here. Traefik splits /api off to the backend before
  # traffic reaches this container — the job the vite preview server's proxy used to do.
}
